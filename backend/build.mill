//| mill-version: 1.0.3

package build

import mill.*, scalalib.*

object implementations extends ScalaModule {
  def scalaVersion = "3.7.1"
  def mvnDeps = Seq(
    mvn"com.lihaoyi::cask:0.10.2",
    mvn"com.lihaoyi::upickle:4.2.1",
    mvn"io.github.kitlangton::neotype:0.3.25",
    mvn"io.github.iltotore::iron:3.1.0"
  )

  object test extends ScalaTests {
    def mvnDeps = Seq(mvn"com.lihaoyi::utest:0.9.0")
    def testFramework = "utest.runner.Framework"
  }
}
